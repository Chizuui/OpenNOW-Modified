# Dependencies
node_modules/

# Lock files
yarn.lock
pnpm-lock.yaml
package-lock.json
!opennow-stable/package-lock.json

# Build outputs
dist/
target/
target2/
target3/
/.cache/
/video/
/docs/research/
# Native streamer build outputs are generated by `npm run native:build`.
# The loader DLLs live next to the platform exe (bin/<platform>/); the flat
# bin/*.dll copies are a legacy layout and must never be tracked again.
native/opennow-streamer/bin/opennow-streamer.exe
native/opennow-streamer/bin/*/*
native/opennow-streamer/bin/*.dll
native/opennow-streamer/bin/.gitkeep

# IDE
.idea/
.vscode/
*.swp
*.swo
.serena/
*.xcuserstate
xcuserdata/

# Xcode build outputs
DerivedData/

# OS
.DS_Store
Thumbs.db
nul

# Logs
*.log

# Environment
.env
.env.local

# Sensitive data
*.tokens.json
gfn_tokens.json

# Test files
test/

# AI Agent guidelines (local only)
AGENTS.md
release-notes.md

# Nix
result
.deriveddata/
.deriveddata-device/
.opencode
vendor/
!native/opennow-streamer/vendor/
!native/opennow-streamer/vendor/**

# Claude
.claude/
claude-plans/
.freebuff/
